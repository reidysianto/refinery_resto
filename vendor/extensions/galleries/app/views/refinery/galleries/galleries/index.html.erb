<% content_for :body do %>

<% end %>
<div class='container'>
  <div class="row">
    <div class='jumbotron'>
        <h2 class="text-center">Food of the day</h2>
        <%= image_tag @page.images.first.url(), class:"img-primary img-responsive"%>     
    </div>
  </div>
</div>

<div class='container'>
  
<% @galleries.each do |t| %>
  <% if t.id % 4 == 0  %>
    <div class="row">
  <%end%>
  <div class='col-md-3 text-center'>
    <%= link_to refinery.galleries_gallery_path(t),class: 'navbar-link' do %>
    <%= image_tag t.image.url(),class: "img-thumbnail img-thumb"%>
    <p class="nav navbar-link"><%= t.name%></p>
    <%end%>
  </div>
<% if t.id % 4 == 0  %>
    </div>
    <hr></hr>    
  <%end%>
<%end%>
</div>

